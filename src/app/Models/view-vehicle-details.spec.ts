import { ViewVehicleDetails } from './view-vehicle-details';

describe('ViewVehicleDetails', () => {
  it('should create an instance', () => {
    expect(new ViewVehicleDetails()).toBeTruthy();
  });
});
