import { UserDashboardView } from './user-dashboard-view';

describe('UserDashboardView', () => {
  it('should create an instance', () => {
    expect(new UserDashboardView()).toBeTruthy();
  });
});
